*.md
*.sh
*.py
.design/

# Print build artifacts, not part of the website. They were being served at
# /exports/_build/*.html with no noindex tag, which made five draft flyers the
# only indexable pages on a domain deliberately kept out of search -- robots.txt
# allows crawling on purpose so crawlers can read the noindex on the real pages.
exports/
